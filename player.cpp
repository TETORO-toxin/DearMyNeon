/*!
 * @file player.cpp
 * @brief Player クラス実装: プレイヤー入力・物理・描画・当たり判定など
 *
 * Player.h に宣言した機能を実装します。Update() で入力解釈、移動量算出、
 * エフェクト更新、弾の更新、当たり判定などを行います。Draw() は UI と
 * スプライト描画を担当します。
 */

#include "player.h"

// player.cpp left intentionally minimal; implementations are split across separate translation units.
